import 'package:flutter/material.dart';

BoxDecoration cameraImageContainerDecoration() {
  return BoxDecoration(
    border: Border.all(
      color: Colors.teal.shade500,
      width: 3,
    ),
    borderRadius: const BorderRadius.all(Radius.circular(23))
  );
}

